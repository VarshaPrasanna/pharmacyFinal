import { GenderImagePipe } from './gender-image.pipe';

describe('GenderImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderImagePipe();
    expect(pipe).toBeTruthy();
  });
});
